package com.mall4j.cloud.api.biz.dto.channels;

import lombok.Data;

@Data
public class EcAttrInfo {
    //属性键（属性自定义用）
    private String attr_key;
    //属性值（属性自定义用）
    private String attr_value;
}
